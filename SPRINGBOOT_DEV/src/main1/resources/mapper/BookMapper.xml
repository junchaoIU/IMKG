<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.openData.dao.mapper.BookMapper">
    <!-- Search all book   -->
    <select id="getAllBook" resultType="com.openData.dao.entity.Book">
        SELECT
            *
        FROM
            book_table
    </select>

</mapper>